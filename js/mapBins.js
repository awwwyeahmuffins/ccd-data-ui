// js/mapBins.js — LEGACY SHIM (deleted in Phase 6, REDESIGN.md §4.2 move
// policy). The named bins + the one plain-language precinct formatter live in
// js/map/mapBins.js.
export * from "./map/mapBins.js";
