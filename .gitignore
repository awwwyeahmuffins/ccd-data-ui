# Dependencies
node_modules/

# OS files
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc
*.pyo
.Python
env/
venv/
.env

# IDE
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
npm-debug.log*

# Build artifacts
dist/
build/

# Temporary files
*.tmp
*.temp

# CDK
infra/cdk.out/
infra/node_modules/
infra/test-results/
cdk.out/
cdk-outputs.json

# Voter PII — never commit or deploy.
# Root-level CSVs are ad-hoc voter exports (VANIDs, names, addresses, phones);
# they are ignored as a class so a `git add -A` cannot sweep one in. Add a
# narrow negation below if a genuinely non-PII root CSV is ever needed.
VoterRegistrationFile.txt
/*.csv

# Operational field-data source workbooks (aggregate CSV outputs are committed)
canvass_source.xlsx

# Generated caches and local artifacts
data/cache/
screenshots/
source_material/
test-results/
playwright-report/
collin_elections_master/

# ethnicity_infer generated artifacts — contain voter PII / derived labels.
# ALL csv output in this dir is voter-derived; only code/config/seed lists
# (.py/.json/.md/seeds/*.txt) may be committed.
data_processor/ethnicity_infer/*.csv
data_processor/ethnicity_infer/*.csv.bak
data_processor/ethnicity_infer/training_data.csv
data_processor/ethnicity_infer/model.joblib
data_processor/ethnicity_infer/*_flagged.csv
